git fetch upstream && git merge upstream/master

git fetch upstream && git rebase upstream/master

I’ll package the `~/coturn/install` directory into a tarball now and then confirm the output file path and size.

Ran terminal command:  cd ~/coturn && tar -czf coturn-install-$(date +%Y%m%d-%H%M%S).tar.gz install && ls -lh coturn-install-*.tar.gz | tail -n 1

Created successfully.

Tarball:
- `~/coturn/coturn-install-20260823-163928.tar.gz`
- Size: `881K`
